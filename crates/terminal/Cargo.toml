[package]
name = "auriga-terminal"
version = "0.1.0"
edition = "2021"

[dependencies]
alacritty_terminal = { workspace = true }
ratatui = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "terminal_benches"
harness = false
